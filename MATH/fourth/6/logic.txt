+ Логика работы с инфиксным выражением, перевод в постфиксную и вычисление выражения та же, что и в 4.5,
только тут работает с деревом выражений вместо стеков.
+ Двойные операторы заменяем на уникальные единичные
+ Логика для получения таблицы с вычислением выражения с разными значениями
- Для каждого варианта n вариаций значений каждой переменной создаем массив char, кладем их туда
- Копируем дерево рекурсивно, на вход функции даем индекс и массив значений пункта выше; 
если встречаем букву, то меняем на соответстующий символ; все остальные операторы / переменные просто копируем.
- В результате даем функции Evaluate new_root и выводим результат функции