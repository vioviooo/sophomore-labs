if (head == NULL) {
                    print_scs(EMPTY);
                } else {
                    print_list(head);
                }


                prev_state = head;
                if ((status = add_liver(&head, prev_state)) != OK) {
                    print_scs(status);
                    flag = false;
                }